1.数据库内容暂时搁置，保存原有数据表的格式和形式不变，先留着
2.所谓的“数据库”是远程文件夹——服务器本地的文件夹，不同的权限设置起来挺麻烦
（目前考虑使用账号和密码访问方式，以实现对普通用户和超级用户访问权限的区分）

目前比较倾向于还是使用数据库做中介，用以区分相应的人员权限，能够设置相应的访问内容。

个人感觉这样实现比较有思路，但是不确定行不行，不确定是否满足老师的思路，我准备先试试：
1在数据库中设置相应的"权限"字段，包括两种权限（超级用户和管理员），两种权限能够在页面中跳转到不同的地方（当然，也可以使用不同的登录节面）
2我认为还是得把文档的地址（应该html格式的生成文档和docx的原始文档都可以）存在相应的数据库数据表里面，该数据库表中加上一列（列名为可访问的人）
3超级用户有权更改用户的相应的文档权限（说白了就是可以添加一段数据项，数据项包括访问的文件名和用户名）
4超级用户设置完成后即可在普通用户界面看到

现在感觉起步还好，能实现一些

当下的思路就是不弄数据表进行数据的条目存储，数据库只做文件和用户配对。

